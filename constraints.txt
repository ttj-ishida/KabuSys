# 依存バージョン制約（サプライチェーンリスク低減）
# 使用方法: pip install -c constraints.txt -r requirements.txt
# 更新方法: pip-compile requirements.txt --output-file constraints.txt
pandas==2.2.3
numpy==1.26.4
duckdb==0.10.3
pyarrow==15.0.2
requests==2.32.3
websocket-client==1.7.0
pyyaml==6.0.1
streamlit==1.35.0
pytest==8.2.2
