# Changelog

すべての重要な変更はこのファイルに記録します。  
このフォーマットは「Keep a Changelog」に準拠します。セマンティックバージョニングを採用しています。

## [Unreleased]
（現在のところ未リリースの変更はありません。）

## [0.1.0] - 2026-03-15
初期リリース。プロジェクトの基本的なパッケージ構成とエントリポイントを追加しました。

### 追加
- パッケージ `kabusys` を追加
  - パッケージ説明（モジュール先頭の docstring）: "KabuSys - 日本株自動売買システム"
  - バージョン情報: `__version__ = "0.1.0"`
  - パブリック API エクスポート: `__all__ = ["data", "strategy", "execution", "monitoring"]`
- サブパッケージのスケルトンを追加
  - `kabusys.data`：データ取得・管理用モジュール（プレースホルダ）
  - `kabusys.strategy`：売買戦略定義用モジュール（プレースホルダ）
  - `kabusys.execution`：注文送信・約定処理用モジュール（プレースホルダ）
  - `kabusys.monitoring`：稼働監視・ログ/メトリクス用モジュール（プレースホルダ）
- パッケージの初期ファイル配置（`src/kabusys/__init__.py` および各サブパッケージの `__init__.py`）

### 変更
- 該当なし（初版のため）

### 修正
- 該当なし（初版のため）

### 削除
- 該当なし（初版のため）

### 既知の制限・注意事項
- 各サブパッケージは現在スケルトン（空の `__init__.py`）の状態で、具体的な機能・実装は未追加です。
- テスト、ドキュメント、CI 設定、依存関係情報等はまだ含まれていません。

### 利用方法（簡易）
- パッケージ情報の確認:
  - Python 実行環境で `import kabusys; print(kabusys.__version__)` によりバージョンが確認できます。
- 提供予定のモジュール（データ取得、戦略、実行、監視）は `from kabusys import data, strategy, execution, monitoring` でインポート可能です（現時点ではプレースホルダ）。

### マイグレーション / アップグレードノート
- 初回リリースのため、互換性に関する注意点はありません。今後のリリースで API を追加・変更する際は、セマンティックバージョニングとこの CHANGELOG にて互換性方針を明示します。

---

貢献者: 初期作成者（自動生成されたコードベースに基づく初期コミット）