@CLAUDE.md
CluaudeCodeに修正を入れたら自動でGithubにプッシュしたいです。またプッシュされたらGithubActionsでChatGPTがプルリクエストをレビューするようにしたいです。

やりたい構成は 十分実現できます。
構成としては次の流れになります。

ClaudeCodeで修正
     ↓
GitHubにPush
     ↓
Pull Request作成
     ↓
GitHub Actions起動
     ↓
OpenAI API（ChatGPT）でコードレビュー
     ↓
PRにコメント投稿

実装は GitHub Actions + OpenAI API で行います。

1. 全体アーキテクチャ
2. 必要なもの
項目	用途
OpenAI API Key	ChatGPT呼び出し
GitHub Token	PRへコメント投稿
GitHub Actions	自動実行
3. リポジトリ設定
Secret登録

GitHub

Settings
  → Secrets and variables
  → Actions

追加

OPENAI_API_KEY
4. GitHub Actions作成
.github/workflows/ai_code_review.yml
name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  review:
    runs-on: ubuntu-latest

    steps:

      - name: Checkout
        uses: actions/checkout@v4

      - name: Get PR diff
        run: |
          git fetch origin ${{ github.base_ref }}
          git diff origin/${{ github.base_ref }} > diff.txt

      - name: Install python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install openai requests

      - name: Run AI Review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          REPO: ${{ github.repository }}
        run: |
          python .github/scripts/ai_review.py
5. AIレビューコード
.github/scripts/ai_review.py
import os
import requests
from openai import OpenAI

client = OpenAI(api_key=os.environ["OPENAI_API_KEY"])

with open("diff.txt") as f:
    diff = f.read()

prompt = f"""
あなたはシニアソフトウェアエンジニアです。

以下のPull Requestの変更をレビューしてください。

レビュー観点
- バグ
- セキュリティ
- 可読性
- 設計
- パフォーマンス

diff:
{diff}
"""

response = client.chat.completions.create(
    model="gpt-5",
    messages=[
        {"role": "system", "content": "You are a senior software engineer."},
        {"role": "user", "content": prompt}
    ]
)

review = response.choices[0].message.content

repo = os.environ["REPO"]
pr = os.environ["PR_NUMBER"]
token = os.environ["GITHUB_TOKEN"]

url = f"https://api.github.com/repos/{repo}/issues/{pr}/comments"

requests.post(
    url,
    headers={"Authorization": f"Bearer {token}"},
    json={"body": review}
)
6. 実行イメージ

PR作成すると

AI Code Review Bot

以下の問題が検出されました

1. nullチェック不足
2. SQLインジェクションの可能性
3. ログ出力が不足

と PRに自動コメントされます。

7. ClaudeCodeとの開発フロー

開発フローはこうなります。

ClaudeCode
   ↓
git commit
   ↓
git push
   ↓
PR作成
   ↓
AIレビュー
   ↓
修正
   ↓
Merge

AIが常に1人レビューしてくれる状態になります。

8. レベルを1段上げるなら（おすすめ）

さらに実務レベルにするとこうなります。

Diffだけではなくファイルも読む
changed files
↓
AIに渡す
ルール付きレビュー

例

レビュー基準

・SOLID原則
・クリーンアーキテクチャ
・セキュリティOWASP
・可読性
コメントを行単位にする
Line Comment Review

GitHub API

/repos/{owner}/{repo}/pulls/{pull_number}/comments
9. 最強構成（実務で使うなら）

もし本気でやるならこの構成が最強です。

ClaudeCode
   ↓
GitHub Push
   ↓
PR作成
   ↓
GitHub Actions
   ↓
AI Reviewer
   ↓
Auto Fix Suggestion
   ↓
AIが修正PR出す

つまり

AIがレビュー + 修正PRまで出す

もしよければですが

あなたは

ClaudeCode

GitHub

AIレビュー

を使っているので、

開発効率が10倍になる構成を作れます。

もし希望あれば次に

「ClaudeCode × GitHub × AI」最強開発環境

を作ります。

これはかなり強くて

AIレビュー
AIテスト生成
AIバグ修正
AIドキュメント生成

まで自動化できます。